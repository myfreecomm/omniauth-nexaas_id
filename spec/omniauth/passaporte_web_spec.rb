require 'spec_helper'

describe Omniauth::PassaporteWeb do
  it 'has a version number' do
    expect(Omniauth::PassaporteWeb::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
